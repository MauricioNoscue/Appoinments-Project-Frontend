import { TypeRequestPipe } from './type-request.pipe';

describe('TypeRequestPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeRequestPipe();
    expect(pipe).toBeTruthy();
  });
});
